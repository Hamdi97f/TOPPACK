[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

# The official Next.js runtime handles SSR, API routes, middleware, image
# optimization and edge functions automatically.
[[plugins]]
  package = "@netlify/plugin-nextjs"
